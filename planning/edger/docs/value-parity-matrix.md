# Matriz de paridade de valor Buntime -> edger

**Origin:** `planning/edger/epics/08-valor-buntime/00-overview.md`

Esta matriz define “mesmo valor” por capacidade observável. Ela não exige copiar
implementação, nomes internos ou decisões específicas de Bun.

## Escala

| Campo | Valores |
|---|---|
| Prioridade | `must`, `should`, `later` |
| Status | `tested`, `partial`, `planned`, `gap`, `deferred` |
| Evidência | Teste automatizado, runbook, checkpoint, validação Browser ou decisão de escopo |

## Capacidades

| Capacidade Buntime | Valor entregue | Contrato edger esperado | Prioridade | Status atual | Owner | Evidência esperada ou atual |
|---|---|---|---|---|---|---|
| Runtime main-thread orchestration | Processo principal orquestra, mas não executa código de app | Orquestrador Rust resolve rota/manifests e entrega `WorkerRef` ao `WorkerPool`; execução acontece via factory/isolate em `edger-worker`/`edger-isolation` | must | tested | Epic 07, Story 08.25 | `edger-worker/tests/integration_pool.rs`; `edger-orchestrator/tests/kind_dispatch_integration.rs`; `edger-orchestrator/tests/pipeline_integration.rs`; `edger-worker/tests/wasm_pool_integration.rs`; `integration_factory_receives_resolved_worker_ref_before_dispatch` prova que o pool cria isolate via factory com `WorkerRef` resolvido e `ExecutionKind` inferido antes do dispatch; JS/TS real passa pelo Deno bridge e Wasm real passa pelo Wasm isolate |
| Worker manifests e entrypoint detection | Apps entram por `manifest.yaml` ou autodiscovery de `index.html`/`index.ts`/`index.js` | Manifest loader multi-dir + inferência de `ExecutionKind` | must | tested | Epic 07.01, Story 08.14 | `edger-orchestrator/tests/manifest_loader.rs`; loader descobre manifesto YAML, `package.json`, diretório direto ou root multi-dir, prioriza autodiscovery `index.html` antes de JS e infere `StaticSpa`, fetch/JS e Wasm |
| Worker addressing, namespace e semver | Operador publica múltiplas versões e namespaces no mesmo runtime | Router resolve `/name`, `/@scope/name`, versões exatas e ranges semver | must | tested | Epic 07.01, Story 08.08, Story 08.22 | `edger-orchestrator/tests/routing_resolution.rs`; `edger-orchestrator/tests/value_parity.rs`; `ManifestIndex` preserva `latest`, versão exata e resolve ranges `semver::VersionReq` como `^1.0.0` e `~1.2.0` para a maior versão habilitada que satisfaz o range |
| Fetch/routes/static SPA contract | Apps servem handlers, rotas e SPAs sob base path | JS/TS via Deno bridge/deno_core target; StaticSpa Rust; `routes` export como contrato explícito | must | tested | Epic 07.04, Story 08.08 | `edger-orchestrator/tests/kind_dispatch_integration.rs`; `edger-orchestrator/tests/value_parity.rs`; Browser em `http://127.0.0.1:19084/todos` |
| Wasm execution | Workloads Wasm rodam sem depender de JS | Wasmtime standalone com WASI deny-by-default e ABI HTTP v1 documentada | should | tested | Epic 07.05 | `edger-isolation/tests/wasm_integration.rs`; `edger-worker/tests/wasm_pool_integration.rs`; `edger-orchestrator/tests/kind_dispatch_integration.rs`; `same_process_serves_deno_and_wasm_workers_from_one_pool` prova coexistência JS/Wasm no mesmo pool; `workers/wasm-hello`; `planning/edger/docs/wasm-abi.md`; host WASI real e ABI request/response em linear memory seguem como follow-up explícito |
| Worker pool LRU e cache hit | Workers persistentes têm warm dispatch e evictions previsíveis | Pool Rust com cache LRU, metrics e lifecycle tipado | must | tested | Epic 04, Epic 07, Story 08.07 | `edger-worker/tests/integration_pool.rs`; `edger-worker/tests/pool_lru.rs`; `edger-orchestrator/tests/metrics_endpoint.rs` |
| Sliding TTL, `idleTimeout`, `maxRequests` | Runtime controla custo, cleanup e reciclagem de worker | TTL deslizante, idle notification, max request retirement e health checks | must | tested | Epic 04, Epic 07.07 | `edger-worker/tests/supervisor_lifecycle.rs`; `edger-worker/tests/metrics_ephemeral.rs`; `edger-worker/tests/integration_pool.rs` |
| Ephemeral concurrency e queue | `ttl=0` não derruba runtime sob burst; excesso recebe 503 | Gate global de concorrência e fila para workers efêmeros | must | tested | Epic 04 | `edger-worker/tests/metrics_ephemeral.rs` |
| APIs de workers | Operador lista, instala/remove ou habilita/desabilita worker | Admin API protegida para inventário e mutações controladas | must | tested | Story 08.02, Story 08.03, Story 08.11 | `edger-orchestrator/tests/admin_workers_plugins.rs`; `edger-orchestrator/tests/security_operational.rs`; `edger-orchestrator/tests/routing_resolution.rs`; enable/disable runtime usa overlay em memória, atualiza inventário e remove worker desabilitado da rota; install/remove e persistência ficam fora desta fatia |
| APIs de plugins/extensões | Operador lista, recarrega e habilita/desabilita capacidades | **removido**: sistema de extensões/registry/hooks e Admin API de extensões saíram no Epic 17 | later | deferred | Epic 17 | Valor antigo preservado só como histórico; o EdgeR atual não gerencia plugins/extensões no runtime. |
| API keys e sessão operacional | Operador autentica o control plane | **reescrito pelo Epic 17.A**: sem criação/revogação de API keys; sessão/admin usam ControlAuth built-in com root-key via arquivo hot-reload e OIDC genérico opt-in | must | tested | Epic 17.A | `/api/admin/*` é protegido por root-key/OIDC; data plane fica aberto; store SQLite e endpoints de API keys foram removidos. |
| OpenAPI/docs de runtime | Operador e agente descobrem contratos sem ler código | Spec machine-readable ou documentação gerada/estática para API edger | should | partial | Story 08.02, Epic 13 | Contrato admin documentado em `docs/developers/06-operacao-e-testes.adoc`; OpenAPI/JSON Schema formal e catálogo de capabilities para agentes seguem no Epic 13 |
| MCP/AI-native authoring | Agentes descobrem capacidades, criam/modificam workers e validam localmente | MCP server local com tools seguras para descoberta, authoring, validação e preparação de commit/PR sem deploy remoto | must | tested | Epic 13, Stories 13.01-13.05 | `edger-mcp` entrega stdio JSON-RPC com `initialize`, `tools/list` e `tools/call`; tools `edger.list_capabilities`, `edger.list_workers`, `edger.inspect_worker`, `edger.write_worker_file`, `edger.validate_local` e `edger.prepare_commit`; `edger-mcp/tests/protocol.rs` prova contratos versionados, redaction de env sensível, descoberta real de manifests, criação controlada sob `workers/`, validação local positiva/negativa e preparação de commit/PR metadata sem deploy remoto; validação stdio manual em `planning/edger/status/evidence/mcp-stdio-2026-06-30.md` confirmou discovery de 17 workers incluindo `cpanel`, dry-run sem escrita e preparação segura de commit/PR |
| CSRF e internal calls | Mutations browser-facing são protegidas; chamadas internas têm bypass controlado | Guard same-origin para browser, clientes CLI autenticados sem `Origin`, e `x-edger-internal` só como bypass CSRF para root | must | tested | Story 08.03, Story 08.24 | `edger-orchestrator/tests/security_operational.rs`; `edger-orchestrator/tests/admin_workers_plugins.rs`; mutações admin atuais cobrem Origin ausente/mismatch/same-origin, internal header sem auth, internal header com root e internal header com key não-root sem elevação; futuras superfícies mutáveis como files/uploads precisam reutilizar o mesmo guard antes de serem marcadas como cobertas |
| Request ID correlation | Cada request é rastreável em resposta, logs e worker | `X-Request-Id` preservado/gerado e propagado | must | tested | Epic 07.06, Story 08.03, Story 08.07 | `edger-orchestrator/tests/health_integration.rs`; `edger-orchestrator/tests/security_operational.rs`; `edger-orchestrator/tests/metrics_endpoint.rs`; `generated_request_id_is_propagated_to_worker_and_response` prova que ID gerado antes do dispatch volta na response e chega ao worker; `worker_dispatch_log_includes_correlation_fields_without_secrets` prova logs com `request_id` e `worker_name` sem segredos; `planning/edger/status/evidence/story-07-06-runtime.txt`; `planning/edger/status/evidence/story-08-07-runtime.txt` |
| Reserved paths e path validation | Runtime impede route hijack e path traversal | `/api`, `/health`, `/.well-known` reservados; entrypoints/assets confinados; `base: ""` não cria shell | must | tested | Epic 07.07, Story 08.03, Story 08.08 | `edger-orchestrator/tests/security_operational.rs`; `edger-orchestrator/tests/value_parity.rs`; `edger-isolation/src/deno/mod.rs` path tests |
| Body/header limits | Payloads e headers não causam exaustão de memória | Limites globais/per-worker, 413/431 e erro tipado | must | tested | Epic 07.07, Story 08.03 | `edger-orchestrator/tests/limits_test.rs`; `edger-orchestrator/tests/security_operational.rs`; `edger-core/tests/wire_roundtrip.rs`; `edger-isolation/tests/wire_limits.rs`; oversized body returns 413 and header count/value violations return 431 before worker dispatch |
| Sensitive env filtering | Workers não recebem segredos acidentalmente | Allowlist/env filtering antes de spawn | must | tested | Story 08.03, Story 08.10 | `cargo test -p edger-isolation wasm::wasi::tests::filters_sensitive_worker_env -- --exact`; `cargo test -p edger-orchestrator --test kind_dispatch_integration deno_backend_injects_only_filtered_manifest_env`; Deno CLI bridge limpa env herdado, injeta `manifest.env` não sensível e filtra padrões sensíveis antes do spawn |
| Durable SQL provider | Apps/plugins usam estado persistente com concorrência | **removido → backend do worker**: `DurableSqlProvider`, providers SQL e service bindings foram removidos no Epic 17.C | later | deferred | Epic 17.C | Worker escolhe seu próprio backend (libSQL, Deno KV, Postgres+PgBouncer ou nenhum) via env/secrets e egress; Epic 09 fica histórico. |
| API key bootstrap store | Auth operacional funciona antes de qualquer plugin/provider de estado | **removido → ControlAuth built-in**: root-key via arquivo hot-reload e OIDC genérico opt-in protegem só `/api/admin/*` | must | tested | Epic 17.A | Sem `EDGER_AUTH_DB`, sem `SqliteApiKeyStore` e sem endpoints de API keys; data plane fica aberto. |
| KV/Kval store com TTL/OCC | Apps compartilham KV com versionamento e expiração | **removido → externo ao EdgeR**: KV/queue não são bindings do runtime | later | deferred | Epic 17.C | Apps devem usar seu próprio backend/SDK; o EdgeR injeta env/secrets e não fornece provider KV. |
| Queue com retry/DLQ | Jobs assíncronos têm fila, backoff e dead-letter | **removido → externo ao EdgeR**: queue provider não existe mais no runtime | later | deferred | Epic 17.C | Filas/retry/DLQ pertencem ao worker ou a serviço externo escolhido pelo app. |
| Search/indexes KV | Busca sobre valores persistidos | Capability separada, não requisito do MVP de valor | later | deferred | Pós-Epic 08 | Decisão explícita após KV base |
| Plugin dependencies e topological order | Extensões iniciam em ordem correta e falham cedo | **removido**: registry/topologia de extensões não existe mais | later | deferred | Epic 17.E | Não há loader/registry de extensões no runtime atual. |
| Hooks request/response/lifecycle | Plugins interceptam pipeline sem acoplar apps | **removido**: hooks foram deletados; observabilidade segue built-in | later | deferred | Epic 17.E | Middleware plugável não faz parte do EdgeR minimalista. |
| Service sharing/providers | Plugins compartilham serviços sem import direto | **removido → env/secrets + egress**: sem providers/bindings explícitos no runtime | later | deferred | Epic 17.C | Compartilhamento de serviço é responsabilidade do worker ou de infraestrutura externa. |
| Hot reload/enable-disable | Operador muda recursos sem restart completo | Workers continuam podendo ser habilitados/desabilitados; extensões e status store de extensões foram removidos | should | partial | Epic 17.E | Hot-reload relevante atual: root-key por arquivo no ControlAuth; extensões não existem mais. |
| Shell routing | Navegação de app vai ao shell quando apropriado | **removido → AppShell externo** (Epic 17.D) | later | deferred | Epic 17.D | O runtime não sniffa navegação para shell; composição fica em AppShell service/worker externo. |
| Micro-frontend menus/catalog | Apps/plugins aparecem em catálogo navegável | **removido do runtime**: menu contribution de extensões e shell/catalog não são contrato atual | later | deferred | Epic 17.D, Epic 17.E | cPanel permanece como interface de control plane; catálogo/AppShell é externo ao core runtime. |
| Gateway/proxy rules | Runtime faz proxy/redirect/cache/rate limit quando configurado | **removido → API Gateway externo** (Epic 17.D) | later | deferred | Epic 17.D | Auth de borda, rate limit, cache, redirects e proxy ficam em Kong/APISIX/Envoy/cloud LB ou no worker. |
| Vhosts/host routing | Host direciona app/plugin sem reconfigurar app | **removido → API Gateway externo** (Epic 17.D) | later | deferred | Epic 17.D | Host routing é responsabilidade da borda externa; o EdgeR mantém roteamento core por worker. |
| Cron/internal jobs | Jobs agendados disparam requests internas | Scheduler Rust com auth interna | should | tested | Epic 07.03 | `edger-orchestrator/tests/cron_scheduler_test.rs` prova dispatch interno de `manifest.cron[]` por scheduler Tokio, `x-edger-internal: true`, root bearer usado apenas no boundary do pipeline e removido antes do worker, skip de worker disabled, erro claro para schedule inválido, shutdown sem novos ticks e contadores Prometheus `edger_cron_executions_total`/`edger_cron_failures_total`; full cron grammar/distribuído fica fora do v1 |
| Health/readiness/liveness | Operação distingue processo vivo de runtime pronto | `/healthz`, `/readyz`, `/livez` e aliases compatíveis | must | tested | Epic 07.06, Story 08.07 | `edger-orchestrator/tests/metrics_endpoint.rs`; Browser local em `http://127.0.0.1:19084/healthz` e `/readyz`; `planning/edger/status/evidence/story-08-07-runtime.txt` |
| Metrics e pool stats | Operador mede hit rate, evictions, latência, erros e workers vivos | `/metrics` Prometheus com snapshot agregado; `/metrics/stats` JSON com pool + workers | must | tested | Epic 07.06, Story 08.07, Story 08.12 | `edger-orchestrator/tests/metrics_endpoint.rs`; `edger-worker/tests/metrics_ephemeral.rs`; `planning/edger/status/evidence/story-07-06-runtime.txt`; `planning/edger/status/evidence/story-08-07-runtime.txt`; `planning/edger/status/evidence/story-08-12-runtime.txt`; `/metrics` expõe contadores HTTP `edger_http_requests_total`, duração HTTP last-observed, pool/cache/spawn/request duration, ephemeral e cron counters; `/metrics/stats` expõe `app`, estado, requests, uptime e unhealthy por worker sem segredos; memória RSS/heap, UI e retenção histórica global ficam fora desta fatia |
| Logging e warnings acionáveis | Falhas de segurança/configuração aparecem nos logs | Logs estruturados com request ID e diagnósticos sem segredos; sem gateway/provider histórico no runtime atual | must | tested | Story 08.07, Story 08.29, Epic 17, Epic 18 | Request ID preservado e propagado; logs operacionais seguem sem Authorization/root key/body. Diagnósticos de gateway/provider eram da arquitetura pré-Epic 17 e não representam a superfície atual. |
| Performance harness | Regressões são medidas por cenário, não por intuição | Baseline local inicial; harness edger opt-in para warm persistent worker | should | partial | Story 08.07, Epic 07.07 | `edger-orchestrator/tests/perf_harness.rs`; `planning/edger/docs/performance-baselines.md`; `planning/edger/status/evidence/story-08-07-runtime.txt`; harness ignorado mede p50/p95 e hit rate para worker persistente in-memory; cenários slow/ephemeral/burst seguem evolução |
| Filesystem/deploy layout | Operador sabe onde ficam apps, plugins, estado e backup | Runbook local verificado por gate; PVC/K8s como later tier | must | tested | Story 08.07, Story 08.26, Story 08.27 | `docs/developers/06-operacao-e-testes.adoc` cobre env vars, probes, metrics, status store de extensões, backup local e troubleshooting; `planning/edger/scripts/deploy-layout-check.py` valida roots de workers, estado local, status store de extensões, probes, Admin API, backup e gates; `run-gates.sh` grava `deploy-layout-check.txt`; deploy remoto/PVC/K8s permanece fora do escopo v1 |
| Provas de migração | Fluxos reais confirmam valor entregue | Suite/runbook de `todos`, auth, estado, shell/gateway e lacunas explícitas de cron/proxy | must | tested | Story 08.08 | `edger-orchestrator/tests/value_parity.rs`; Browser em `http://127.0.0.1:19084/todos`; `planning/edger/status/evidence/story-08-08-runtime.txt` |
| UI administrativa final | Operação visual completa | cPanel/admin UI sobre contratos atuais do control plane, protegida por root-key/OIDC | should | partial | Epic 12, Epic 17, Epic 18 | `workers/cpanel` permanece como Static SPA do control plane. Superfícies antigas de extensões/gateway/API keys foram removidas; a UI atual cobre sessão, workers e métricas/pool do runtime minimalista. UI final completa e WebIDE continuam como evolução futura. |
| Marketplace completo | Distribuição pública de extensões | Registro estático, manifesto assinado/validado e upload controlado primeiro | later | deferred | Epic 10, Epic futuro | Marketplace público permanece fora desta fase; o Epic 10 deve criar inventário/manifesto operacional suficiente para não fechar a porta para distribuição futura |
| Full Node/Next.js sem adapters | Compat ampla de ecossistema Node | Tiers explícitos de compat, não promessa must-have | later | deferred | Pós-Epic 08 | Decisão após execução JS embutida |

## Regras de atualização
- `tested`: há teste, runbook ou evidência manual versionada no repo.
- `partial`: o valor existe em parte, com lacuna explícita.
- `planned`: story definida, sem evidência executada.
- `gap`: sem contrato aceito ainda.
- `deferred`: decisão consciente fora do escopo desta fase.

## Relação com compatibilidade técnica
`planning/edger/docs/compat-matrix.md` mede comportamento técnico. Esta matriz mede
valor de produto e operação. Uma linha só fecha quando as duas visões não se
contradizem.

## Fontes usadas na classificação inicial
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/concepts/runtime.md`
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/concepts/worker-pool.md`
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/concepts/plugin-system.mdx`
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/concepts/storage.md`
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/ops/security.md`
- `/Users/djalmajr/Developer/djalmajr/buntime/apps/site/src/content/docs/ops/performance.md`
