# Matriz de compatibilidade Buntime ↔ edger

**Status:** atualizado pelo Epic 24 com frameworks Deno SSR e deploy real
**Origin:** `planning/edger/design.md` (Migration notes, mapping table)

Legenda: `pending` = ainda não testado | `tested` | `partial` | `gap`

Paridade de valor fica em `planning/edger/docs/value-parity-matrix.md`. Esta matriz responde se o comportamento técnico é compatível; a matriz de valor responde se o fluxo operacional/produto equivalente está entregue.

| Comportamento Buntime | edger | Status | Teste / notas |
|---|---|---|---|
| Worker addressing `/name`, `/@scope/name@ver`, ranges semver | orchestrator router | tested | `crates/edger-orchestrator/tests/routing_resolution.rs`; `crates/edger-orchestrator/tests/value_parity.rs`; suporta `latest`, versão exata e `semver::VersionReq` como `^1.0.0` e `~1.2.0` |
| Runtime orchestration boundary | worker pool + isolation factory | tested | `crates/edger-worker/tests/integration_pool.rs`; `crates/edger-orchestrator/tests/kind_dispatch_integration.rs`; `WorkerPool` recebe `WorkerRef` resolvido, cria isolate por factory injetada e despacha pelo kind inferido; o orchestrator não executa código de app diretamente |
| Console stdout/stderr consultável | captura multiprocess + store operacional | tested | `crates/edger-isolation/tests/console_capture.rs`; linhas de 4 KiB, 100 linhas/s por stream, canal 1.024, enqueue não bloqueante, redaction de secrets/caminhos, identidade por versão/process ID e recycle; Browser validou 92 registros reais do `commonjs` sem vazamento de path local |
| Manifest fields (mapping table design) | edger-core manifest | partial | `crates/edger-core/tests/models_mapping.rs`; complex Buntime fields still tracked by value matrix |
| Entrypoint autodiscovery priority | manifest loader | tested | `crates/edger-orchestrator/tests/manifest_loader.rs`; `manifest.yaml`/`package.json` permanecem fontes explícitas, e autodiscovery sem manifesto escolhe `index.html` antes de `index.ts/js/mjs` |
| `fetch(req) -> Response` contract | Deno CLI bridge in isolation | tested | `crates/edger-orchestrator/tests/kind_dispatch_integration.rs` |
| Worker-local `deno.json` / import map | Deno CLI bridge cwd + `--config` | tested | `deno_backend_loads_worker_deno_config_import_map`; `serve` manual curl |
| Response body finite stream | Processo persistente streama por frames tagueados (H/C/E) | tested | `crates/edger-isolation/tests/streaming.rs::finite_multi_chunk_stream_delivers_whole_body`; body finito chega inteiro, multi-chunk |
| Infinite/SSE stream | **Passthrough real por frames** (16.D): chunks fluem worker → UDS → pool → axum `Body::from_stream` → cliente | tested | `crates/edger-isolation/tests/streaming.rs::sse_stream_delivers_chunks_incrementally` + `crates/edger-orchestrator/tests/streaming_e2e.rs` (SSE incremental de ponta a ponta, disconnect recicla, paridade buffered); validado ao vivo: `curl -N /sse` recebe 1 evento/s incrementalmente e `/stream` flui indefinidamente. Disconnect mid-stream recicla o worker (socket desincronizado → processo fresco); erro mid-stream fecha a resposta sem travar; byte cap alto no harness como guarda de runaway (`EDGER_STREAM_MAX_BYTES`, truncamento limpo); stall entre frames limitado pelo timeout do worker |
| Remote `https://` imports | Deno bridge + processo persistente | tested | `logger-stdout` manual curl returned 200; sandbox 2026-07-03: processo persistente usa `allowNet` do manifest antes de `EDGER_DENO_ALLOW_NET`, mantém rede aberta se nenhum for configurado e permite negar com `allowNet: []`; `DENO_DIR` fica por worker por padrão (`denoCacheMode: per-worker`) para `npm:`/`jsr:` sem cache compartilhado entre workers |
| Express (`npm:express`) | Deno process + node:http listener capture | tested | `framework_compat.rs::express_and_hono_execute_on_the_process_backend` (ignored: needs deno+npm); fixture `workers/examples/express-demo`; rota JSON + `:param` via captura de `app.listen()` no harness persistente |
| Hono (`npm:hono`) | Deno process + `Deno.serve` capture | tested | mesmo teste; fixture `workers/examples/hono-demo`; rota JSON + `:param` via `Deno.serve(app.fetch)` |
| NestJS + Express | `nodeHttpProxy: true`; servidor real em socket Unix privado | tested | NestJS 11.1.6: DI/decorators, provider warm, guard, interceptor, DTO/`ValidationPipe`, rota parametrizada e `StreamableFile`; `framework_compat.rs::nestjs_express_and_fastify_cover_enterprise_http_features`; install ZIP 201 e processo live `healthy` sem recycle |
| NestJS + Fastify | `nodeHttpProxy: true`; `FastifyAdapter` sobre socket Unix privado | tested | Mesma prova NestJS 11.1.6 com o adaptador Fastify: todas as rotas e streaming passaram, estado warm preservado; install ZIP 201 e processo live `healthy` sem recycle |
| Fastify 5 | `nodeHttpProxy: true`; lifecycle Node real, hooks e schema | tested | Fastify 5.6.1: hook, estado warm, rota `:param`, validação JSON e stream; `framework_compat.rs::fastify_and_koa_cover_server_lifecycle_and_middleware`; install ZIP 201, processo live persistente |
| Koa 3 | `nodeHttpProxy: true`; middleware Node real em socket Unix privado | tested | Koa 3.0.1: middleware onion/upstream, `AsyncLocalStorage`, router, body parser, validação e stream; mesmo teste; install ZIP 201, processo live persistente |
| **Fullstack blessed path: Hono SSR + JSX** | Deno process; TSX transpilado nativamente (`deno.json` `jsxImportSource: hono/jsx`), `jsxRenderer` | tested | `framework_compat.rs::hono_ssr_jsx_renders_html_on_the_server`; fixture `workers/examples/ssr-demo` (deploy de FONTE `.tsx`, zero build); página SSR + API JSON no mesmo worker; validado ao vivo no preview (SSR warm ~1.8ms). Caminho fullstack recomendado do EdgeR |
| SvelteKit (`@sveltejs/adapter-node` build) | Deno process + captura `node:http`; wrapper restaura a base | tested | fixture `workers/examples/sveltekit-demo` validada no browser (network+console limpos, hidratação OK): página SSR, rota `+server.ts`, assets `_app/immutable/*`. **Receita**: (1) `paths: { base: '/sveltekit-demo', relative: false }` no build — paths RELATIVOS (default) quebram a hidratação na URL bare `/name` (o browser resolve `./_app` contra a raiz → 404, achado em teste manual no DevTools); (2) `wrapper.mjs` que importa o `handler.js` exportado pelo build e restaura a base via `x-base` antes de delegar (o EdgeR entrega path relativo). Harness: captura de todos os listeners `request` + default de Host (achados do spike) |
| TanStack Start (build Vite) | `kind: fullstack`, `adapter: tanstack`; Deno process + Rust asset preflight | tested | fixture `workers/examples/tanstack-demo`: SSR `/`/`/about`, server route `/api/info`, assets, hidratação e navegação client-side revalidados ao vivo em 2026-07-17. Story 19.05 removeu o wrapper `index.mjs`: manifest usa `ssrEntrypoint: server/server.js`, `clientDir: client`, `basePath: auto`; Rust serve `/assets/*` e arquivos públicos, rejeita traversal/malformed, pula `/api/*` e `/_serverFn/*`, e restaura `x-base` antes do SSR. Testes in-memory: `tanstack_fullstack_worker_dispatches_ssr_with_base_restored`, `tanstack_fullstack_worker_serves_client_asset`, `tanstack_fullstack_worker_rejects_bad_asset_paths` |
| Next.js standalone | `adapter: nextjs`; Deno Node compat + servidor `node:http` real em Unix socket privado | tested | Next 16.2.10 `output: standalone`, App Router, HTML SSR e API 200 após install ZIP 201; teste `node_http_proxy_preserves_real_request_and_response_semantics`; sem porta TCP/allow-net para o proxy interno. Receita em `frameworks-deno-ssr.md` |
| Nuxt + Nitro | `adapter: nuxt`; preset `deno_deploy` + `Deno.serve` | tested | Nuxt 4.4.8/Nitro 2.13.4: SSR 200, API 200, install ZIP 201; handler recebe `ServeHandlerInfo`. Receita em `frameworks-deno-ssr.md` |
| Astro + adapter Deno | `adapter: astro`; entrypoint Deno exporta `handle` e pode capturar `Deno.serve` | tested | Astro 7.1.1/adapter 0.6.0: bundle de produção, SSR 200, API 200, install ZIP 201; teste `exported_handle_entrypoint_serves_astro_style_builds`. Receita em `frameworks-deno-ssr.md` |
| SolidStart + Vinxi/Nitro | `adapter: solidstart`; preset `deno_deploy` + bases Vinxi explícitas | tested | SolidStart 1.3.2/Vinxi 0.5.11: SSR 200, API 200, asset de hidratação 200, install ZIP 201. O template v1 exige `Router base` e bases dos routers no build; receita em `frameworks-deno-ssr.md` |
| Fresh 2 + Vite | `adapter: fresh`; entrypoint de produção Deno preservado no diretório original | tested | Fresh 2.3.3: SSR 200, API 200, island Preact e assets 200, install ZIP 201. O runtime não reloca o bundle, preservando o `import.meta.dirname` usado pelo cache estático, e continua limitando a leitura ao worker |
| Lume 3 | `adapter: lume`; build `_site` servido pelo Rust sem processo JS | tested | Lume 3.1.2: raiz, rota de diretório `/about/` e CSS 200, install ZIP 201; resolução cobre `index.html`, directory index e `.html` limpo e injeta a base do worker |
| Remix / React Router framework mode | `adapter: remix`; servidor Node real por socket Unix privado + assets Rust | tested (experimental) | React Router 8.0.0: SSR com loader 200, API JSON 200, asset hidratável 200 e install ZIP 201. Usa entry server Web Streams e Express autocontido; permanece experimental conforme a lista oficial do Deno Deploy |
| Ingress (rate limit / cache / redirects / host routing) | **removido → API Gateway externo** (Epic 17) | n/a | política de borda saiu do edger; ver `planning/edger/epics/17-edger-minimalista` |
| Micro-frontend / AppShell | **removido → AppShell externo** (Epic 17) | n/a | shell routing (sniff `sec-fetch-dest`) removido; composição via AppShell service/worker; catch-all por `HomepageFallback` + worker fallback (ver `follow-ups/fallback-worker-routing.md`) |
| CommonJS Node server examples | Node `http.createServer` adapter | partial | Simple `commonjs` tested; Buntime-compatible mounted paths strip worker base and expose `x-base`, so standalone Hono route `/commonjs-hono` resolves at `/commonjs-hono/commonjs-hono` |
| Mounted worker base path | Relative request path + `x-base` | tested | Mirrors Buntime `createWorkerRequest`; namespaced worker test asserts `/api/ping` + `x-base: /@team/checkout` |
| `routes` export | Deno CLI bridge routes dispatch | tested | `routes_table_worker_dispatches_by_path_method_and_params` + `routes_table_without_fallback_returns_404_for_unmatched` em `kind_dispatch_integration.rs`; exact > `:param` > `*` wildcard, method map (405), fallback `fetch`, 404 sem fallback; fixture `workers/examples/routes-demo` |
| Wasm standalone worker | wasmtime `WasmIsolate` | tested | `crates/edger-isolation/tests/wasm_integration.rs`; `crates/edger-isolation/src/wasm/handler.rs` unit tests; ABI v2 writes the request frame to guest linear memory via `edger_alloc`, invokes `edger_handle`, decodes the response frame, links WASIp1 with FS/rede denied by default, rejects arbitrary host imports, and keeps the existing JS/Wasm coexistence path |
| Static file read from JS worker | Deno CLI bridge | tested | `serve-html` E2E |
| Static SPA (`entrypoint: index.html`) | Rust `serve_static_spa` | tested | `crates/edger-orchestrator/tests/manifest_loader.rs`; `workers` fixture + manual `buntime/apps/todos` HTTP validation: manifest-less discovery, index/assets/favicon/fallback + `<base href="/todos/">` |
| `visibility: public` worker access | **removido → data plane aberto** (Epic 17) | n/a | Auth de worker saiu do edger; data plane recebe request cru e o worker decide sua própria auth. |
| ApiKeyPrincipal + namespaces | **removido → auth do worker externa** (Epic 17) | n/a | Namespace-gate/API-key principal não existe mais no data plane; autorização de app é responsabilidade do worker ou de gateway externo. |
| Root key bypass | ControlAuth built-in | tested | Root-key via `EDGER_ROOT_KEY_FILE` protege somente `/api/admin/*`, com hot-reload e sem store SQLite; OIDC genérico opt-in também cobre o control plane. |
| Admin API key create/revoke | **removido → root-key/OIDC** (Epic 17) | n/a | Endpoints de gestão de API keys foram removidos; operação usa root-key de arquivo e/ou OIDC no control plane. |
| API key file-backed bootstrap store | **removido → root-key file hot-reload** (Epic 17) | n/a | `EDGER_AUTH_DB`/`SqliteApiKeyStore` foram removidos; não há bootstrap store persistente de API keys no runtime. |
| Admin mutation CSRF/internal guard | orchestrator admin API | tested | `crates/edger-orchestrator/tests/security_operational.rs`; `crates/edger-orchestrator/tests/admin_workers_plugins.rs`; browser-originated mutations exigem same-origin, CLI/API com Bearer funciona sem `Origin`, e `x-edger-internal: true` não autentica nem eleva keys não-root |
| Worker runtime enable/disable | manifest index/admin API | tested | `crates/edger-orchestrator/tests/admin_workers_plugins.rs`; `crates/edger-orchestrator/tests/security_operational.rs`; `crates/edger-orchestrator/tests/routing_resolution.rs`; overlay em memória remove worker disabled da rota e inventário mostra `disabled`/`loaded` |
| Extension runtime enable/disable | **removido** (Epic 17) | n/a | `ExtensionRegistry`, Admin API de extensões e overlay/status store de extensões foram removidos do runtime. |
| publicRoutes bypass | **removido → data plane aberto** (Epic 17) | n/a | `publicRoutes` deixou de afetar dispatch; o data plane não é autenticado pelo edger. |
| Sliding TTL / ephemeral ttl=0 | worker pool | tested | `crates/edger-worker/tests/supervisor_lifecycle.rs`; `crates/edger-worker/tests/metrics_ephemeral.rs`; `crates/edger-worker/tests/integration_pool.rs` |
| maxRequests cap | supervisor | tested | `crates/edger-worker/tests/supervisor_lifecycle.rs`; `crates/edger-worker/tests/metrics_ephemeral.rs` |
| onRequest hooks order + short-circuit | **removido** (Epic 17) | n/a | Hooks de extensão e middleware registrável foram removidos; observabilidade permanece built-in. |
| Reserved paths `/api`, `/health` | router | tested | `crates/edger-orchestrator/tests/routing_resolution.rs`; `crates/edger-orchestrator/tests/value_parity.rs`; paths reservados continuam protegidos sem shell routing. |
| Ingress body/header limits | orchestrator wire layer | tested | `crates/edger-orchestrator/tests/limits_test.rs`; body acima de `MAX_BODY_BYTES` retorna 413 sem dispatch, mais de 100 headers retorna 431, e valor acima de 8 KiB retorna 431. Admin deploy/upload/download usa cap separado de 64 MiB; ZIP expandido fica limitado a 256 MiB/50.000 entradas. Next ZIP de 11 MiB instalado e baixado ao vivo |
| Empty plugin `base: ""` | manifest index | tested | `crates/edger-orchestrator/tests/value_parity.rs`; empty base does not register an app surface; shell resolver was removed by Epic 17. |
| Env filtering sensitive patterns | worker env | tested | `edger-isolation` WASI filtering test; `crates/edger-orchestrator/tests/kind_dispatch_integration.rs`; Deno child env is cleared before spawn and only filtered manifest env is injected |
| Worker stats snapshot | worker pool + metrics API | tested | `crates/edger-worker/tests/metrics_ephemeral.rs`; `crates/edger-orchestrator/tests/metrics_endpoint.rs`; `/metrics/stats` returns pool + workers JSON, while `/metrics` remains aggregate Prometheus text with pool, HTTP and cron counters |
| HTTP request metrics | orchestrator metrics middleware | tested | `crates/edger-orchestrator/tests/metrics_endpoint.rs`; `/metrics` exposes `edger_http_requests_total{method,status}` and `edger_http_request_duration_ms_last` after real requests |
| Request-id/log correlation | request-id middleware + tracing logs | tested | `crates/edger-orchestrator/tests/metrics_endpoint.rs`; generated request ids are inserted before worker dispatch and echoed in the response, while dispatch logs include `request_id` and `worker_name` without Authorization/root key/body values |
| Eventos operacionais locais | store bounded + Admin API + cPanel Logs | tested | `crates/edger-orchestrator/tests/observability_api.rs`; `/api/admin/observability/events` é root-only, retém 2.000 eventos globais/200 por identidade, pagina por cursor e filtra por namespace/worker/version/source/level/outcome/status/request ID/trace ID; dispatch real preserva o mesmo `x-request-id`; mensagens potencialmente sensíveis são redigidas; o workspace do worker consulta esses eventos sem OTEL |
| Cron internal requests | native scheduler 07.03 | tested | `crates/edger-orchestrator/tests/cron_scheduler_test.rs`; scheduler snapshots enabled `manifest.cron[]` jobs at startup, validates schedule v1, dispatches in-process HTTP with `x-edger-internal: true` + root bearer credential at the pipeline boundary, strips root auth before worker serialization, exposes `edger_cron_*` Prometheus counters, and cancels tasks on shutdown; full cron grammar/distributed leader election remain outside v1 |
