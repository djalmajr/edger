# Plano: EdgeR 0.3.2 (em rcs)

## Context

- **Origem:** o operador pediu a 0.3.2 com as melhorias levantadas depois da
  0.3.1, entregues em rcs (2026-09-25).
- **Fontes:**
  - os pedidos do planner em produção (`planner.djalmajr.dev`);
  - as lacunas em `follow-ups/planner-deploy-gaps-2026-09-22` (ai-memory);
  - `follow-ups/bumps-de-dependencia.md`.
- **Decisões:** registradas em `follow-ups/0.3.2-decisoes.md` (D1 a D12).

## Files

- **rc.1:**
  - `crates/edger-orchestrator/src/manifest_index_stub.rs`,
    `tests/routing_resolution.rs` e `tests/deploy_install.rs`;
  - `crates/edger-orchestrator/src/server.rs` e
    `crates/edger-orchestrator/src/bin/edger.rs`;
  - `Cargo.lock`, `.github/workflows/ci.yml`, AGENTS.md (CLAUDE.md), docs e
    CHANGELOG;
  - `.agents/`.
- **rc.2:** `crates/edger-orchestrator/src/pipeline.rs`, `router.rs`,
  `metrics.rs`, `workers/core/*/manifest.yaml`, docs.
- **rc.3:** `crates/edger-core/src/config.rs`,
  `crates/edger-isolation/src/fullstack.rs`, `multiproc.rs`,
  `deno/cli.rs` e `deno/bundle.rs`; o skill `edger-deploy`.
- **rc.4:** `Cargo.toml`, `Dockerfile`, `Dockerfile.cross`,
  `.github/workflows/ci.yml`, os call sites do rusqlite, e a imagem do bun.

## Details

- **Troca sem downtime (D2, D3):**
  - `host_routes` passa a mapear host → nome.
  - Cada `ManifestEntry` guarda os hosts que declara.
  - `worker_for_host` resolve a versão servida pelo nome e exige que essa
    versão declare o host.
- **Bind (D4):** `EDGER_BIND`, com padrão `0.0.0.0`.
- **Domínio do app (D6):** a checagem de dono do Host roda antes das rotas
  fixas.
- **Métricas (D7):** `observability:read` em `/metrics*`.

## Tasks

- [x] **rc.1:**
  - [x] host → nome;
  - [x] `EDGER_BIND`;
  - [x] patches de dependência e actions;
  - [x] docs e CHANGELOG;
  - [x] ajustes `.agents` (D9);
  - [x] gate;
  - [x] revisão;
  - [x] tag `v0.3.2-rc.1` (publicada e no labdev).
- [x] **rc.2:**
  - [x] domínio com dono inteiro do app;
  - [x] `/metrics` com chave;
  - [x] versão dos core workers (levantamento antes);
  - [x] docs;
  - [x] tag (publicada e na VPS).
- [ ] **rc.3:**
  - [ ] arquivos públicos do tanstack;
  - [ ] `--no-config`;
  - [ ] import de tipo no bundle;
  - [ ] follow-up de `allowNet`;
  - [ ] basePath fora da raiz com TanStack Start Solid (D26);
  - [x] tag (publicada e na VPS).
- [ ] **rc.4:**
  - [ ] toolchain nos cinco lugares;
  - [ ] rusqlite 0.40;
  - [ ] bun 1.4;
  - [x] tag (publicada e na VPS).
- [ ] **0.3.2:**
  - [ ] consolidação do CHANGELOG;
  - [ ] PR, merge e tag (confirmação).

## Verification

- **Por rc:**
  - `cargo test --workspace && cargo clippy --workspace -- -D warnings && cargo fmt -- --check`;
  - revisão de outra família;
  - CI verde;
  - smoke no labdev depois do upgrade (confirmação).
- **rc.1:**
  - teste de integração com instalação staged no mesmo host, promote e
    rollback, sem nenhuma resposta 404 no host entre os passos;
  - teste de colisão entre nomes diferentes.
