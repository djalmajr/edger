# 0.3.2: decisões e alternativas para validação

Demanda: melhorias da 0.3.2, entregues em rcs (pedido do operador em
2026-09-25). Plano: `.agents/plans/0.3.2.md`. Não há issue no Linear para o
edger; este arquivo é o registro. O operador valida tudo de uma vez.

## D1. Sequência de rcs
- **Decisão:**
  - rc.1: troca de versão sem downtime, `EDGER_BIND` e bumps de patch.
  - rc.2: domínio com dono inteiro do app, e `/metrics` com chave.
  - rc.3: lacunas de framework (arquivos públicos do tanstack,
    `--no-config`, import de tipo no bundle).
  - rc.4: dependências que exigem código.
  - O backup (export/import) fica para a 0.3.3.
- **Por quê:** cada rc muda uma superfície, o que permite testar no labdev e
  voltar atrás sem misturar causas. O pedido do planner (troca sem downtime)
  vem primeiro.
- **Alternativas:**
  - Uma rc só: mais rápido, mas mistura roteamento, segurança e dependências
    numa validação.
  - Backup já na 0.3.2: é o item mais caro e atrasa o resto.
- **Reverter:** baixo. A ordem das rcs pode mudar enquanto nada foi
  publicado.
- **Onde:** `.agents/plans/0.3.2.md`.
- **Status:** aplicada no branch `release/0.3.2` (rc.1 local; publicação em D11).

## D2. Domínio pertence ao nome do app
- **Decisão:** `hosts:` liga o domínio ao **nome** do worker. Responde a
  versão que o nome serve no momento: o ponteiro de default ou, sem ele, a
  maior versão habilitada e não staged. Isso só vale se o manifesto dessa
  versão declarar o domínio; caso contrário o domínio não resolve.
- **Por quê:**
  - `promote` passa a trocar o domínio de forma atômica, e o rollback é
    promover a anterior.
  - Desabilitar a versão atual faz a anterior assumir, em vez de dar 404.
  - Uma versão staged continua sem assumir o domínio antes do promote. O
    teste
    `staged_public_release_stays_pinned_until_promoted_across_rescan_and_restart`
    exige isso.
- **Alternativas:**
  - Domínio = união dos `hosts:` de todas as versões, servido pelo default:
    uma staged que declara um domínio novo passaria a ligá-lo, servido por
    uma versão que não o declara.
  - Manter por versão (hoje): cada deploy apaga e reinstala, com o site fora
    do ar.
- **Reverter:** médio. A mudança fica no índice de manifests, mas o
  comportamento de deploy muda para quem já usa `hosts:`.
- **Onde:** `crates/edger-orchestrator/src/manifest_index_stub.rs`. Testes em
  `tests/routing_resolution.rs` e `tests/deploy_install.rs`.
- **Status:** aplicada no branch `release/0.3.2` (rc.1 local; publicação em D11).

## D3. Colisão de domínio só entre apps diferentes
- **Decisão:**
  - Uma versão de outro nome que declare um domínio já reivindicado recebe
    `409 COLLISION` (como hoje).
  - Versões do mesmo nome podem repetir o domínio.
  - A reivindicação vale desde a instalação, mesmo staged.
- **Por quê:** a instalação staged precisa reservar o domínio para o promote
  ser atômico, e nenhum outro app pode tomá-lo no meio da troca.
- **Alternativas:** só reservar ao promover. Assim outro app poderia pegar o
  domínio entre a instalação staged e o promote.
- **Reverter:** baixo.
- **Onde:** mesmo arquivo de D2.
- **Status:** aplicada no branch `release/0.3.2` (rc.1 local; publicação em D11).

## D4. Variável de bind: `EDGER_BIND`
- **Decisão:**
  - `EDGER_BIND` recebe um endereço IP (v4 ou v6), com padrão `0.0.0.0`.
  - Valor inválido faz o processo falhar no start com mensagem clara.
  - A porta continua em `PORT`.
  - O comando de dev do AGENTS.md e os docs passam a usar `127.0.0.1`.
- **Por quê:**
  - Em dev, o control plane com `ROOT_API_KEY=test-root` fica aberto na rede
    local.
  - `HOST` foi descartado: o zsh e muitos contêineres definem `HOST` com o
    hostname, e o processo quebraria ou escutaria no lugar errado.
  - O padrão continua `0.0.0.0` para não quebrar o contêiner numa versão de
    patch.
- **Alternativas:**
  - Padrão loopback: mais seguro, mas quebra quem roda o binário em
    contêiner sem o chart.
  - `BIND_ADDR`: sem o prefixo do projeto.
- **Reverter:** baixo.
- **Onde:** `crates/edger-orchestrator/src/server.rs`,
  `crates/edger-orchestrator/src/bin/edger.rs`, AGENTS.md, docs.
- **Status:** aplicada no branch `release/0.3.2` (rc.1 local; publicação em D11).

## D5. Dependências por rc
- **Decisão:**
  - **Na rc.1:** os patches `tokio` 1.53.1, `bytes` 1.12.1, `thiserror`
    2.0.21 e `futures-core` 0.3.34, e as actions `docker/login-action` v4 e
    `docker/metadata-action` v6.
  - **Na rc.4:** a toolchain (#51 e #60 juntos, nos cinco lugares de
    `follow-ups/bumps-de-dependencia.md`), rusqlite 0.40 (#55) e bun 1.4
    (#59).
- **Por quê:**
  - Os patches não pedem código.
  - A toolchain subindo só no Dockerfile deixaria o compilador do build
    diferente do compilador do CI.
  - rusqlite e bun falham no CI e pedem código.
- **Alternativas:** mergear os PRs do Dependabot direto no main. Isso exige
  confirmação de merge e desalinha a toolchain.
- **Reverter:** baixo.
- **Onde:** `Cargo.lock`, `.github/workflows/ci.yml`.
- **Status:** rc.1 aplicada no branch; a parte da rc.4 está na fila.

## D6. Domínio com dono é inteiro do app (rc.2)
- **Decisão:**
  - Quando o Host tem dono, nenhuma rota do control plane responde nele: `/`,
    health/ready, `/metrics*`, `/api/*` e `/.well-known/*` vão para o app.
  - O control plane só responde em hosts sem dono: o domínio administrativo,
    o labdev em `/apps` e o IP do pod nos probes.
  - Não haverá prefixo `/_edger/`.
- **Por quê:**
  - Hoje a home do app redireciona para o cPanel.
  - O app não pode ter `/api` nem `/.well-known` (OIDC, ACME, deep links).
  - A Admin API fica exposta no domínio público.
  - O prefixo só serviria para administrar pelo domínio do app, e o planner
    já bloqueia isso no Traefik.
- **Alternativas:**
  - Prefixo `/_edger/` em todo host: mantém a superfície exposta.
  - Hosts administrativos configurados: exige config em toda instalação.
  - Porta separada: muda o chart e o acesso do MCP remoto.
- **Reverter:** médio. Quem hoje administra pelo domínio de um app precisa
  passar a usar um host sem dono.
- **Onde:** `pipeline.rs` e `router.rs`.
- **Status:** na fila (rc.2).

## D7. `/metrics` exige chave (rc.2)
- **Decisão:** `/metrics` e `/metrics/stats` passam a pedir Bearer com
  `observability:read` (ou a root).
- **Por quê:**
  - Hoje expõem nome e versão de cada app instalado. Isso foi conferido no
    labdev, em `/apps/metrics`.
  - O chart não tem ServiceMonitor, então nenhum scrape padrão quebra.
- **Alternativas:**
  - Flag que reabre sem chave: mantém uma porta aberta por configuração.
  - Público só no host admin: o labdev continua expondo.
- **Reverter:** baixo.
- **Onde:** `pipeline.rs` e `metrics.rs`.
- **Status:** na fila (rc.2).

## D8. Versão do cPanel acompanha a release: investigar antes
- **Decisão:** fica fora da rc.1. Na rc.2, entra depois de um levantamento
  de como overlays e ponteiros de core workers usam a versão.
- **Por quê:** `workers/core/cpanel/manifest.yaml` continua `0.3.0`, embora o
  cPanel tenha mudado. Trocar a versão sem saber se overlays e defaults são
  por versão pode deixar customizações órfãs.
- **Alternativas:** subir já, junto com a release. É rápido, mas o risco no
  overlay de quem customizou o cPanel é desconhecido.
- **Reverter:** baixo.
- **Onde:** `workers/core/*/manifest.yaml`, job `publish`.
- **Status:** na fila (rc.2).

## D9. Ajustes da herdr-agents pedidos pela sessão skills
- **Decisão:**
  - Apagar `.agents/herdr-roles/documenter.md`, que é uma cópia antiga com
    precedência sobre o papel da skill.
  - Adicionar `role.reviewer.args=-c sandbox_workspace_write.network_access=true`
    ao `.agents/herdr-agents.conf`.
- **Por quê:**
  - A cópia bloqueia a diretiva nova do documentador (tabela de afirmações
    com fonte).
  - Sem rede, o revisor não conferiu o Rancher na rc.6 da 0.3.1.
- **Alternativas:**
  - Manter a cópia e editá-la: duplica o papel da skill.
  - Rede para todo codex (`args.codex`): daria rede também a quem edita.
- **Reverter:** baixo.
- **Onde:** `.agents/`.
- **Status:** aplicada (commit da rc.1).

## D10. Registro fora de issue pública
- **Decisão:**
  - O plano e as decisões ficam neste repositório.
  - A issue do GitHub seria abertamente pública (o repositório é público).
    Por isso ela fica para quando o PR da rc.1 for aberto, com a confirmação
    de push.
- **Por quê:** issue pública é publicação, e o registro já existe no
  repositório.
- **Alternativas:** abrir a issue já: publica o plano antes da validação do
  operador.
- **Reverter:** baixo.
- **Onde:** este arquivo e `.agents/plans/0.3.2.md`.
- **Status:** bloqueada (confirmação de push e PR).

## D11. Publicação das rcs e deploy no labdev
- **Decisão:** o trabalho da rc.1 fica local, no branch `release/0.3.2`. Push,
  tag `v0.3.2-rc.1`, publish e upgrade do labdev esperam confirmação.
- **Por quê:** a regra global exige confirmação para push, tag e deploy.
- **Alternativas:** nenhuma.
- **Reverter:** —
- **Onde:** branch `release/0.3.2`.
- **Status:** bloqueada (confirmação).

## D12. Follow-up de `allowNet` com socket Unix desatualizado
- **Decisão:** atualizar `follow-ups/scoped-allownet-uds.md` na rc.3, depois
  de um teste que prove que `allowNet` com escopo sobe no backend
  persistente.
- **Por quê:** o código já acrescenta `unix:<socket>` à permissão
  (`deno_sandbox_policy.rs`, desde o #45), mas o follow-up ainda diz que a
  combinação falha.
- **Alternativas:** apagar o follow-up sem teste: afirmação sem prova.
- **Reverter:** baixo.
- **Onde:** `follow-ups/scoped-allownet-uds.md`.
- **Status:** na fila (rc.3).

## D13. Arestas de `windows-sys` no `Cargo.lock` da rc.1
- **Decisão:** aceitar as sete arestas de `windows-sys` que o
  `cargo update -p tokio -p bytes -p thiserror@2.0.18 -p futures-core`
  reatribuiu (0.61.2 → 0.52.0/0.60.2 em `errno`, `rustix`,
  `rustls-platform-verifier`, `tempfile`, `winapi-util`, `nu-ansi-term` e
  `socket2`).
- **Por quê:**
  - São dependências condicionais de Windows, e o projeto não builda para
    Windows: o `Dockerfile` e o `Dockerfile.cross` só geram
    `x86_64-unknown-linux-gnu`, e o CI roda em Linux.
  - Os blocos dessas versões já existiam no lock.
  - O `cargo deny` passa.
- **Alternativas:**
  - Editar o lock à mão para manter 0.61.2: é frágil, e o próximo
    `cargo update` desfaz.
  - Investigar a causa exata no resolver: custo sem efeito nos alvos.
- **Reverter:** baixo.
- **Onde:** `Cargo.lock`.
- **Status:** aplicada.

## D14. Espelho `planning/edger/AGENTS.md`
- **Decisão:** o comando de dev do espelho (`planning/edger/AGENTS.md:15`)
  recebe o mesmo `EDGER_BIND=127.0.0.1` do `CLAUDE.md`. O `AGENTS.md` da raiz
  é um symlink para o `CLAUDE.md` e já ficou atualizado.
- **Por quê:** o espelho repete o comando e ficaria divergente.
- **Alternativas:** deixar como está: o espelho passa a ensinar o bind
  aberto na rede.
- **Reverter:** baixo.
- **Onde:** `planning/edger/AGENTS.md`.
- **Status:** aplicada.

## D15. `EDGER_BIND` com bytes não UTF-8 falha no start
- **Decisão:** ler com `var_os`. Um valor não UTF-8 vira erro de start, e não
  cai no padrão `0.0.0.0`.
- **Por quê:** é um achado P2 da revisão. Um valor inválido nunca pode
  resultar em escuta em todas as interfaces.
- **Alternativas:** ignorar valores não UTF-8, que é o comportamento atual
  e silenciosamente inseguro.
- **Reverter:** baixo.
- **Onde:** `crates/edger-orchestrator/src/server.rs`.
- **Status:** aplicada; re-revisão sem achados.
